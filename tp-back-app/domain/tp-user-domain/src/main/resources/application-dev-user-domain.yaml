spring:
  profiles:
    active: local
    include:
      - common

---
spring:
  config:
    activate:
      on-profile: local-user-domain

---
spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: prod

---
spring:
  config:
    activate:
      on-profile: common